version: '3'

services:
  order-service:
    build: ./
    ports:
      - "4000:8080"
    environment:
      OTEL_TRACES_EXPORTER: "logging"
      OTEL_METRICS_EXPORTER: "logging"
      OTEL_LOGS_EXPORTER: "logging"